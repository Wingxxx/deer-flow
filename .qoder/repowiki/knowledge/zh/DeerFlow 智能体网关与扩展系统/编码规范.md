- 采用 `_installed` 或 `_APPLIED` 全局标记位确保扩展模块（如数据采集、敏感词过滤）的幂等初始化，防止重复挂载。
- 在 `deerflow_entry.py` 中通过显式静态导入（Explicit Static Imports）解决 PyInstaller 对动态加载模块（如各类 LLM Provider 和 Tools）的追踪缺失问题。
- 网关中间件遵循“故障关闭”（Fail-closed）原则，如 `AuthMiddleware` 默认拒绝未认证请求，确保系统在配置缺失时的安全性。