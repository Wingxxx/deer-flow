- **环境初始化**：执行 `make install` 并行安装前后端依赖；运行 `make setup-sandbox` 预拉取沙箱容器镜像。
- **开发启动**：使用 `make dev` 启动带热重载的开发服务，或通过 `make docker-start` 拉起完整的 Docker 开发环境。
- **配置管理**：通过 `make config` 生成本地配置，利用 `make doctor` 诊断系统环境与工具链完整性。