- 所有 ORM 模型类统一以 `Row` 后缀命名（如 `RunRow`, `FeedbackRow`），以区分领域模型与数据库行对象。
- 结构化扩展字段统一采用 `{field_name}_json` 命名并映射为 `JSON` 类型，默认值为空字典或列表。
- 时间戳字段统一使用带时区的 `datetime` 类型，并通过 `lambda: datetime.now(UTC)` 设置默认值以确保时间一致性。