- **核心模型**：`AppConfig` 作为根配置对象，聚合模型、工具、数据库等子配置模块。
- **加载机制**：支持从 `config.yaml` 加载，具备环境变量解析、版本校验及默认值填充；通过全局单例和 `ContextVar` 实现缓存、自动热重载及运行时上下文隔离。
- **路径体系**：`Paths` 类统一管理宿主与沙箱间的路径映射，支持目录隔离及跨平台兼容性与安全校验。
- **扩展配置**：`ExtensionsConfig` 独立管理 MCP 服务器和技能状态，采用 JSON 格式存储并支持独立热重载。