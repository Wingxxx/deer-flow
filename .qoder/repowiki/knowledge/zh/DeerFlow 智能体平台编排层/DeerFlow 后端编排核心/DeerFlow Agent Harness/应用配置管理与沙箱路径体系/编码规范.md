- 所有配置子模块均定义为继承自 `pydantic.BaseModel` 的类，并利用 `Field` 提供详细描述和默认值。
- 配置加载采用“懒加载单例”模式，通过 `get_xxx_config()` 函数暴露全局访问点，并在内部维护缓存状态。
- 敏感或动态配置值在 YAML/JSON 中使用 `$ENV_VAR` 语法引用，由配置类的 `resolve_env_variables` 方法统一递归解析。
- 涉及文件系统路径的操作均通过 `Paths` 类封装，严禁在业务逻辑中直接拼接路径字符串，以确保沙箱挂载点的一致性。