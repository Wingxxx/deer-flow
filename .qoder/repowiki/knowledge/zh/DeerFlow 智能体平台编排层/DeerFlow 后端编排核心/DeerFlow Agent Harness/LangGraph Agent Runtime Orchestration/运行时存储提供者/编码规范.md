- 所有具体的存储后端导入（如 `InMemoryStore`, `SqliteStore`）均采用延迟导入（Lazy Import）模式，仅在工厂方法内部根据配置类型触发。
- 在切换不同存储后端时，均通过 `try...except ImportError` 捕获异常并提供明确的安装指引错误信息。
- 同步与异步提供者均遵循“配置驱动”原则，优先读取 `checkpointer` 配置，若缺失则自动降级为 `InMemoryStore` 并记录警告日志。