- 所有持久化后端（SQLite/Postgres）在实例化后均立即调用 `setup()` 方法以初始化 schema。
- 对可选的第三方依赖（如 langgraph-checkpoint-sqlite/postgres）采用 try-except ImportError 模式，并提供包含安装命令的详细错误提示。
- SQLite 路径处理统一委托给 `_sqlite_utils` 中的 `resolve_sqlite_conn_str` 和 `ensure_sqlite_parent_dir` 工具函数。