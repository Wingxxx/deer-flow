- 所有存储实现均继承自 `abc.ABC` 定义的 `RunEventStore`，确保接口契约的一致性。
- 存储操作均采用异步方法签名（`async def`），其中 I/O 密集型后端（如 JSONL）通过 `asyncio.to_thread` 将同步文件操作卸载至线程池。
- 事件记录统一包含 `thread_id`、`run_id`、`seq`（严格递增序列号）及 `category` 字段，以支持多维度的检索与排序。