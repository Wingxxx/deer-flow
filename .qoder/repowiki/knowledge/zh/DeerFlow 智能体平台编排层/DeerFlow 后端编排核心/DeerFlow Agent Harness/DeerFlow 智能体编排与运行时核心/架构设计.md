- **工厂组装模式**：`create_deerflow_agent` 作为纯参数入口，支持全量接管或基于 `RuntimeFeatures` 的自动装配。
- **动态中间件编排**：实现 `@Next`/`@Prev` 锚点定位算法，允许外部中间件在固定执行链中精确插入并保持依赖顺序。
- **共享状态契约**：定义 `ThreadState` 作为跨模块通信载体，利用 LangGraph 的 Reducer 机制处理沙箱 ID、待办事项及视觉数据的并发合并。
- **运行时特性注入**：根据功能开关（如子代理、长期记忆）动态绑定对应的中间件实例与专用工具，实现执行能力的按需扩展。