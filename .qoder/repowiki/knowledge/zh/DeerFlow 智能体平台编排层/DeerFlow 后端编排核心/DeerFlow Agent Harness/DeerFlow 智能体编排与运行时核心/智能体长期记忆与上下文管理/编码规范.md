- 所有持久化操作均通过 `agent_name` 参数实现全局记忆与特定智能体记忆的物理隔离。
- 采用“读取-修改-写回”模式，在更新内存数据时先深拷贝当前状态，确保原子性与缓存一致性。
- 在消息预处理阶段自动剥离 `<uploaded_files>` 等会话级 ephemeral 标签，防止临时文件路径污染长期记忆。
- 事实提取遵循严格的置信度分级标准，显式用户陈述标记为 0.9+，隐含推断标记为 0.7-0.8。