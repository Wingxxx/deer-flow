- 中间件链遵循严格的执行顺序，澄清中间件（ClarificationMiddleware）必须位于链条末尾以拦截最终请求。
- 提示词模板采用 XML 标签（如 `<role>`, `<thinking_style>`）进行结构化分段，以提高 LLM 的指令遵循能力。
- 运行时配置通过 `RunnableConfig` 的 `configurable` 和 `context` 字段合并传递，支持动态覆盖模型名称和功能开关。
- 技能（Skills）和子代理（Subagents）的描述信息在提示词生成时动态加载，并根据运行时参数（如 `subagent_enabled`）条件性注入。