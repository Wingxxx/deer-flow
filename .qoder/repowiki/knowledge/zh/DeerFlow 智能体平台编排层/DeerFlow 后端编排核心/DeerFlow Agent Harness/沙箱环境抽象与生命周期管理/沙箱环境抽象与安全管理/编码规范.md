- 所有文件路径操作必须经过 `_reject_path_traversal` 检查，禁止包含 '..' 段以防止目录遍历攻击。
- 本地沙箱模式下，输出内容需通过 `mask_local_paths_in_output` 处理，将主机绝对路径替换为虚拟路径以隐藏宿主环境结构。
- 沙箱提供者采用单例模式，并通过 `reset_sandbox_provider` 和 `shutdown_sandbox_provider` 提供显式的状态重置与资源清理接口。