- 在文件读写操作中，通过 `_agent_written_paths` 集合追踪由沙箱写入的文件，仅在读取这些文件时执行路径反向解析，避免修改用户上传或外部生成的原始内容。
- 所有涉及路径转换的方法（如 `_resolve_paths_in_command`）均优先匹配最长路径前缀，防止因路径包含关系（如 `/mnt/skills` 与 `/mnt/skills-extra`）导致的错误替换。
- 在执行系统命令或文件 I/O 时，统一捕获 `OSError` 并将其重新抛出为携带原始容器路径的错误，向调用者隐藏宿主机的真实物理路径以增强安全性。