- 所有技能名称必须遵循 kebab-case 规范（小写字母、数字和连字符），且长度不超过 64 个字符，通过正则表达式在入口处统一校验。
- 自定义技能的写入操作采用“先写临时文件再替换”的原子性策略，确保在写入过程中不会损坏原有技能文件。
- 技能目录结构严格区分为 `public`（只读内置）和 `custom`（可编辑用户）两类，并通过 `SkillCategory` 枚举进行管理。
- 支持文件的路径访问实施严格的沙箱校验，禁止使用 `..` 进行目录穿越，且必须位于 `references`, `templates`, `scripts`, `assets` 等白名单子目录下。