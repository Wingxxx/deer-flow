- 以 `types.py` 中的 `Skill` 数据类为跨子模块共享的核心契约，统一了技能的元数据结构。
- `installer.py` 作为编排中枢，协调 `security_scanner.py`（内容审查）、`parser.py`（元数据提取）与 `storage/`（持久化）完成从归档到落地的闭环。
- 采用异步 I/O 与线程池结合的策略处理耗时的安全扫描任务，确保主流程的非阻塞性。
- 通过 `permissions.py` 强制执行文件系统沙箱权限，在安装完成后自动剥离写权限，实现物理层面的隔离保护。