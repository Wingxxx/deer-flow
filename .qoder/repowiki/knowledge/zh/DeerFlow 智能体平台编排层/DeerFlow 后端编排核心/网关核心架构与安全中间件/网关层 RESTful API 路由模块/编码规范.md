- 所有路由模块均使用 `APIRouter(prefix="/api", tags=[...])` 初始化，以保持 URL 路径前缀和 OpenAPI 标签的一致性。
- 涉及敏感操作或资源访问的端点普遍采用 `@require_permission` 装饰器进行细粒度的权限校验与所有者检查。
- 请求与响应数据结构严格继承自 `pydantic.BaseModel`，并利用 `Field` 提供元数据描述及默认值配置。