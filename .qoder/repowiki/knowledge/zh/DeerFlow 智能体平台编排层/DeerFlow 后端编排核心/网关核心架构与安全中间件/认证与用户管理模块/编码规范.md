- 所有涉及I/O或CPU密集型的密码操作（哈希、校验）均提供 async 包装器，通过 asyncio.to_thread 避免阻塞事件循环。
- 采用版本化密码哈希格式（$dfv2$...），在验证时自动检测版本并支持旧版本透明迁移，登录时触发 opportunistic rehash。
- 认证失败统一返回 None 或特定的 TokenError 枚举，避免在底层泄露具体的失败原因（如区分用户不存在与密码错误）。
- 使用 StrEnum 定义错误代码和令牌错误类型，确保错误标识在序列化时保持字符串格式，便于API响应标准化。