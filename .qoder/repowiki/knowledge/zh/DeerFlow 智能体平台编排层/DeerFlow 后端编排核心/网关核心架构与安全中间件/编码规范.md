- 所有非公开路径的请求必须经过 AuthMiddleware 进行身份校验，未通过则直接返回 401，实现“默认拒绝”的安全策略。
- 路由器通过 deps.py 提供的依赖注入函数获取运行时单例（如 RunManager, Store），禁止直接访问 app.state。
- 用户身份信息通过 contextvar (deerflow.runtime.user_context) 在全局范围内传递，确保底层仓库能自动应用所有者过滤。