- 所有基础设施单例（如 Checkpointer, Store）必须通过 `asynccontextmanager` 工厂函数创建，并在 `lifespan` 中统一管理生命周期。
- 业务逻辑层（Routers/Workers）严禁直接访问全局配置或数据库引擎，必须通过 `deps.py` 中的依赖注入函数或 `RunContext` 获取所需资源。
- 跨模块配置共享采用 `AppConfig` 模型作为唯一事实来源，禁止在各子模块中重复定义配置解析逻辑。