- API 层函数统一从 `@/core/config` 获取基础 URL 并调用 `@/core/api/fetcher` 中的 fetch 方法。
- 消息工具函数优先通过 `message.type` 和 `additional_kwargs` 或 `tool_calls` 字段识别消息语义（如推理、工具调用）。
- 子任务状态更新逻辑在 `context.tsx` 中通过引用追踪防止终端状态下的重复渲染通知。