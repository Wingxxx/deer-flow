- 采用分层架构：`types.ts` 定义 `MCPConfig` 与 `MCPServerConfig` 数据模型；`api.ts` 封装基于 `fetcher` 的后端交互逻辑（GET/PUT `/api/mcp/config`）；`hooks.ts` 基于 `@tanstack/react-query` 提供 `useMCPConfig` 查询与 `useEnableMCPServer` 突变钩子，并在更新后自动失效缓存。
- `index.ts` 作为模块入口，统一导出 API 函数与类型定义，但未导出 hooks，暗示 hooks 可能需在更高层级或直接引用文件使用。