- 所有涉及状态变更（POST/PUT/DELETE/PATCH）的请求必须通过 `readCsrfCookie` 读取 Cookie 并注入 `X-CSRF-Token` Header。
- API 客户端实例采用单例模式缓存，通过 `Map` 区分默认与 Mock 环境，避免重复初始化开销。
- 在 SSR 环境下访问 `document` 或 `window` 对象时，必须先进行 `typeof` 检查以确保代码安全性。