- 所有 API 请求均通过自定义 fetcher 发起，并使用 readErrorDetail 统一从响应中提取后端错误信息。
- 写操作（上传、删除）成功后，强制使对应的列表查询键 ['uploads', 'list', threadId] 失效以保持数据一致性。
- 文件验证逻辑采用白名单/黑名单模式，通过 splitUnsupportedUploadFiles 将文件流拆分为 accepted 和 rejected 两组。