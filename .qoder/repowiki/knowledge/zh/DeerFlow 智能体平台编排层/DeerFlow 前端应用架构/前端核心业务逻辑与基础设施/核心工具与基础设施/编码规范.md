- 所有工具函数均采用纯函数或无副作用设计，便于单元测试和跨组件复用。
- 涉及国际化的函数（如 `formatTimeAgo`）优先从Cookie或检测逻辑获取Locale，并允许调用方显式传入以覆盖。
- 文件扩展名映射采用集中式的 `Record<string, string>` 字典结构，便于维护和扩展新类型。
- 博客文章处理遵循“收集-合并-排序”模式，先按语言收集再按Slug合并，最后统一按日期排序。