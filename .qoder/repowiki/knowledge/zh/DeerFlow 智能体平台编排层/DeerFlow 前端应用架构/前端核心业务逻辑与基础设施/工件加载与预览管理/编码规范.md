- 采用协议前缀（如 `write-file:`）区分实时草稿内容与持久化文件路径，并在加载逻辑中进行分支处理。
- HTML 预览增强功能（如 Base HREF 和滚动恢复）通过正则匹配 `<head>` 或 `<body>` 标签安全注入脚本，若已存在则跳过以避免重复。
- 工具调用参数解析统一使用 `URL` 对象处理包含 `message_id` 和 `tool_call_id` 的伪文件路径字符串。