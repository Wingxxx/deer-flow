- 采用 `useI18n` 自定义钩子作为访问翻译字典 `t` 和当前区域 `locale` 的唯一入口。
- 区域偏好通过名为 `locale` 的 Cookie 进行持久化，有效期设置为 1 年。
- 翻译字典采用强类型接口 `Translations` 定义，确保多语言文件（如 `en-US.ts`）的结构一致性。
- 服务端与客户端共享区域标准化逻辑 `normalizeLocale`，确保区域代码（如 `zh-CN`）的统一处理。