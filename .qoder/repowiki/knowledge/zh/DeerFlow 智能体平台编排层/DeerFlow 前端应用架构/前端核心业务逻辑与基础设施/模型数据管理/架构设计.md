- 采用分层架构：`types.ts`定义数据契约，`api.ts`封装底层HTTP请求并处理静态模式降级，`hooks.ts`基于@tanstack/react-query提供声明式数据获取逻辑。
- `index.ts`作为统一出口，聚合导出API与类型定义，屏蔽内部文件结构。
- 依赖方向清晰：Hook依赖API层，API层依赖配置模块（`core/config`）与静态模式检测（`core/static-mode`）。