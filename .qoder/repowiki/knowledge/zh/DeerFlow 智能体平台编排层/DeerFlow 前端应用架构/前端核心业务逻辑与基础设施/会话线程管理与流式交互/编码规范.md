- 采用乐观更新策略，在 `useThreadStream` 中维护 `optimisticMessages` 状态以提升用户输入反馈的即时性。
- 通过 `messageIdentity` 函数为消息生成唯一标识符，并在合并历史与实时消息时执行去重逻辑以防止渲染重复内容。
- 利用 `useRef` 存储异步回调中的最新状态（如 `threadIdRef`、`listeners`），避免闭包陷阱并确保事件处理的准确性。
- 在数据导出模块中实施防御性过滤，默认排除隐藏消息、工具调用细节及内部推理标记，确保导出内容的纯净度。