- 通用容器抽象：通过 `Section` 组件统一封装各区块的标题（Title）、副标题（Subtitle）及内边距样式，确保页面视觉节奏的一致性。
- 客户端边界标记：涉及浏览器 API、交互动画或状态管理的组件（如 Hero, SandboxSection）均在文件顶部显式声明 "use client"。
- 样式合并规范：普遍使用 `cn()` 工具函数处理 `className` 属性，支持外部传入的类名与内部默认样式的条件合并。