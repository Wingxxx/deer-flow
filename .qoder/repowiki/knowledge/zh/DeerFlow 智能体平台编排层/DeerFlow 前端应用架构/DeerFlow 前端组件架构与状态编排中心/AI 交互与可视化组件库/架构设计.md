- **分层架构**：基于 React Context 实现状态共享（如 `Context`, `MessageBranch`, `Plan`），将逻辑层与表现层解耦。
- **核心依赖**：深度集成 `ai` (Vercel AI SDK) 处理流式数据与消息类型，使用 `@xyflow/react` 构建节点画布 (`Canvas`, `Node`, `Edge`)。
- **渲染引擎**：采用 `streamdown` 进行 Markdown 流式渲染，并结合 `shiki` 实现客户端代码高亮。
- **UI 基础**：建立在 shadcn/ui 之上，通过组合模式封装出具有 AI 业务语义的原子组件（如 `ContextTrigger`, `CodeBlockCopyButton`）。