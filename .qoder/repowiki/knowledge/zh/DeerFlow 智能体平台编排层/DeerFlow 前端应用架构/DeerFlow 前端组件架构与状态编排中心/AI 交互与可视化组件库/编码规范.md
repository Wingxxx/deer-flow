- 广泛采用 Compound Components（复合组件）模式，通过 React Context 在父组件（如 `Context`, `MessageBranch`）与子组件间共享状态。
- 所有组件均遵循 "use client" 指令，确保在 Next.js App Router 中作为客户端组件运行以支持交互与 Hooks。
- 统一使用 `cn` 工具函数合并 Tailwind CSS 类名，并支持通过 `className` 属性覆盖默认样式。