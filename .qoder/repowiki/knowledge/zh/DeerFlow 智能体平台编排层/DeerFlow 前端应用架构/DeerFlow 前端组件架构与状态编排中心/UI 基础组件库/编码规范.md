- 所有组件均使用 `cn(...)` 工具函数合并默认类名与用户传入的 `className`，确保 Tailwind 样式的可覆盖性。
- 复合组件及其子部件普遍使用 `data-slot="..."` 属性标识 DOM 结构，便于外部选择器定位或样式定制。
- 涉及客户端交互或浏览器 API 的组件（如 Dialog, Tooltip, Galaxy）均在文件顶部声明 `"use client"` 指令。
- 组件变体（Variant）和尺寸（Size）逻辑通过 `cva` 函数集中定义，并导出对应的 `xxxVariants` 常量供外部复用。