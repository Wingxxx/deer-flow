- **统一鉴权与路由守卫**：通过 `layout.tsx` 在服务端执行 `getServerSideUser`，根据认证状态（authenticated/unauthenticated/needs_setup）决定渲染内容或重定向，确保子模块仅在安全上下文中运行。
- **共享布局编排**：利用 `workspace-content.tsx` 封装 `SidebarProvider` 和 `QueryClientProvider`，为 `agents` 和 `chats` 子模块提供一致的侧边栏导航、命令面板（CommandPalette）及 Toast 通知环境。
- **入口流量分发**：根 `page.tsx` 根据环境变量（如静态演示模式）动态重定向至首个对话线程或新建页面，实现工作区默认行为的集中控制。