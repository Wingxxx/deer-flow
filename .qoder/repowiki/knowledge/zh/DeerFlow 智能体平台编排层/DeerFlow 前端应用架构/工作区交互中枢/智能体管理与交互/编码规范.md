- 在新建智能体等关键异步操作后，采用带指数退避的重试机制（如 `getAgentWithRetry`）以应对后端写入延迟。
- 使用自定义错误类（如 `AgentNameCheckError`）区分网络不可达、API 禁用或请求失败等不同异常场景，以便 UI 层提供精准反馈。
- 在卡片等受限容器中展示长文本时，结合 CSS 截断与动态检测的 `TruncatedTooltip`，仅在内容溢出时显示完整提示。