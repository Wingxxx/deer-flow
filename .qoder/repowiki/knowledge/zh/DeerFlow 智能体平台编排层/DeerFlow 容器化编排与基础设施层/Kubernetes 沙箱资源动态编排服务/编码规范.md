- 采用幂等设计模式，在创建沙箱前先检查同名 Service 是否存在，若存在则直接返回现有状态而非重复创建。
- 支持存储后端灵活切换，通过检测 `SKILLS_PVC_NAME` 或 `USERDATA_PVC_NAME` 环境变量，动态决定使用 HostPath 还是 PersistentVolumeClaim 进行数据卷挂载。
- 实施严格的输入校验与路径安全处理，利用正则表达式限制 `thread_id` 和 `user_id` 格式，并使用 `pathlib` 兼容不同操作系统的路径拼接以防范路径遍历风险。