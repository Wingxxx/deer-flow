- 所有 API 代理位置块均显式设置 `proxy_http_version 1.1` 及标准转发头（Host, X-Real-IP, X-Forwarded-For, X-Forwarded-Proto）。
- 涉及流式输出或大文件上传的接口（如 langgraph, uploads）统一禁用代理缓冲（proxy_buffering off）以避免权限错误或响应延迟。
- 前端代理位置块（location /）统一启用 WebSocket 支持，通过设置 Upgrade 和 Connection 头实现协议升级。