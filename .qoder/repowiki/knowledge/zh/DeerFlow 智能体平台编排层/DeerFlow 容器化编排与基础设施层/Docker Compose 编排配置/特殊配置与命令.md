- **启动命令**：生产环境使用 `make up`，开发环境使用 `docker-compose -f docker-compose-dev.yaml --env-file ../.env up -d`。
- **Windows 适配**：在 Windows 环境下必须设置 `DEER_FLOW_ROOT` 为 `/c/...` 格式路径以兼容 Docker Desktop 的 MSYS2 路径转换。