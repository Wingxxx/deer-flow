- 所有服务均通过自定义 bridge 网络（deer-flow/deer-flow-dev）互联，并配置 `restart: unless-stopped` 确保高可用。
- 敏感配置（如 CLI 凭证）默认不挂载，仅通过可选的 overlay compose 文件或环境变量（如 `CLAUDE_CODE_OAUTH_TOKEN`）注入。
- Nginx 配置中禁用 `proxy_buffering` 并设置长超时（600s），以支持 Gateway 的 SSE 流式响应与长连接。