- **启动命令**：开发环境使用 `docker-compose -f docker-compose-dev.yaml --env-file ../.env up -d`；生产环境通过 `make up` 触发。
- **环境变量依赖**：强依赖根目录 `.env` 文件定义路径映射（如 `DEER_FLOW_ROOT`）及认证密钥（如 `BETTER_AUTH_SECRET`），Windows 环境下需特别注意 MSYS2 风格的路径格式（`/c/...`）。