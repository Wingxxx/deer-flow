- 遵循“扩展目录优先”原则，所有自定义功能优先在 `frontend/extensions/` 或 `deerflow_extensions/` 中实现，严禁直接修改官方核心源码。
- 核心源码补丁必须采用 `try/except ImportError` 包裹并配合幂等守卫（如 `_installed`），确保扩展缺失时系统仍能正常启动。
- Fork 同步必须以 Release Tag 为节点进行 Cherry-pick，并在同步后执行 20 个侵入点的完整性验证与合并残留扫描。
- 所有 Level 2/3 级别的源码改动必须在 `docs/patches/` 记录补丁详情，并在 `docs/changelog/code_change_summary/` 归档变更记录。