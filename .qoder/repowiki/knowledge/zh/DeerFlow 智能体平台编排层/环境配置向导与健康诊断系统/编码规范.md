- 健康检查脚本采用统一的 `CheckResult` 数据类封装检查结果，包含状态（ok/warn/fail）、详情及修复建议，并通过 `print()` 方法统一输出格式。
- 向导步骤函数遵循 `run_<step>_step` 命名规范，返回特定的 `dataclass` 结果对象，实现步骤间的数据解耦与传递。
- 所有 Python 脚本均包含跨平台兼容性处理，如 `check.py` 中针对 Windows 的 `pnpm.cmd` 探测及 UTF-8 输出流重配置。