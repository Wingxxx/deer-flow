- 所有扩展模块的 `startup.py` 均使用全局布尔变量（如 `_installed`）实现安装逻辑的幂等性保护，防止重复注入。
- 扩展功能通过统一的 `try/except ImportError` 模式进行包裹，确保在依赖缺失或扩展不可用时 DeerFlow 主干仍能正常运行。
- 涉及外部状态修改的操作（如 `.env` 文件写入）均使用 `filelock.FileLock` 进行并发控制，避免多进程环境下的资源竞争。