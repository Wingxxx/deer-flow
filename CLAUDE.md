# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

**DeerFlowApp** — A minimal uni-app (Vue 3) shell that wraps a DeerFlow server in a full-screen WebView and packages it as an iOS/Android app via HBuilderX cloud build. No business logic, no native plugins, no third-party SDK dependencies.

## Project Structure

```
uni-app/
├── DeerFlowApp/DeerFlowApp/        # HBuilderX-generated uni-app project
│   ├── config.js                   # 🎯 唯一 Server URL 来源 - 编译时配置
│   ├── pages/index/index.vue       # WebView + 加载画面 + 错误覆盖层 + 配置面板 + 悬浮按钮 + 扫码
│   ├── App.vue                     # App lifecycle hooks (onLaunch/onShow/onHide)
│   ├── main.js                     # Vue 3 SSR app creation (HBuilderX default)
│   ├── pages.json                  # Route config: single page at pages/index/index, custom nav style
│   ├── manifest.json               # App config (HTTP cleartext, Barcode模块, 相机权限, App Icon)
│   ├── uni.scss                    # Uni-app SCSS variables (default theme)
│   ├── uni.promisify.adaptor.js    # Makes uni API calls return Promises
│   ├── index.html                  # Web entry point (used by H5 platform)
│   ├── generate-icons.ps1          # 从 static/app-icon.png 一键生成所有尺寸图标
│   └── static/
│       ├── app-icon.png            # 🎯 唯一 App 图标源文件（1024x1024, PNG）
│       └── brand.png               # 启动加载画面品牌图片
│   └── unpackage/
│       └── res/icons/              # 图标生成产物（由 generate-icons.ps1 生成，gitignore）
├── docs/
│   └── superpowers/
│       ├── bug-fix-log.md          # 🐛 Bug 修复记录
│       ├── plans/                  # Implementation plans
│       └── specs/                  # Design specifications
└── .trae/
    ├── documents/                  # Execution plan (Trae IDE agent artifacts)
    └── specs/                      # Formal spec-driven development artifacts
```

## Development Workflow

This project is **HBuilderX-based** (not CLI-based). There are no `package.json`, `npm`, or CLI build scripts.

### Prerequisites
- HBuilderX (App development edition) — download from https://www.dcloud.io/hbuilderx.html
- Android: Developer mode + USB debugging enabled on phone
- iOS (optional): Apple Developer account ($99/year)

### Running on Device
1. Open `DeerFlowApp/DeerFlowApp` folder in HBuilderX
2. Connect phone via USB
3. Click `Run → Run to Phone or Simulator → Android/iOS`

### Building for Distribution
- **Android**: HBuilderX → `Publish → Native App Cloud Build` → Android (.apk)
  - Package name: `com.deerflow.app`
  - Certificate: DCloud default certificate
- **iOS**: HBuilderX → `Publish → Native App Cloud Build` → iOS (.ipa)
  - Requires `.p12` certificate + `.mobileprovision` provisioning profile

## Key Configuration

### Server URL — config.js（唯一权威来源）
`config.js` 是唯一的服务器地址来源。**不设存储持久化**，扫码/手动输入的 URL 只在当前会话生效。

```js
export default {
  serverUrl: 'http://192.168.1.56:2026/'  // 编译时默认值，修改后需重新云打包
}
```

### Server URL — 运行时配置（应用内修改）
App 启动后，点击右下角的 **⚙ 悬浮按钮**（`plus.nativeObj.View` 原生绘制层）弹出配置浮层。配置浮层仅显示当前地址（只读），提供「测试连接」和「扫码自动绑定」两个操作。

#### 三层 URL 安全防护
1. **格式校验**：实时检查 URL 格式，自动补全 `http://` 协议前缀，拦截空格/中文等非法字符
2. **内网检测**：检测到公网域名（如 baidu.com）时提示用户「DeerFlow 服务器通常位于内网」
3. **服务器身份验证**（硬限制）：向 `{url去掉末尾斜杠}/health` 发送 GET 请求，满足任一条件即判为有效：
   - `service === "deer-flow-gateway"` 且 `status === "healthy"`
   - `detail.code === "not_authenticated"`（需登录）
   - HTTP 状态码 401 或 403

#### 交互流程
```
⚙ 按钮（原生 Native 层）→ 隐藏 WebView → 弹出配置浮层（Vue DOM）
  → 当前地址（只读显示）
  → 点击「🔌 测试连接」→ GET /health 验证（去末尾斜杠防双斜杠）
    ├─ 验证通过 → 绿色 ✅
    └─ 验证失败 → 红色 ⛔ → 可查看/清空调试日志
  → 点击「📷 扫码自动绑定」→ 扫码 → /health 验证 → 直接加载
  → 关闭面板 → 恢复 WebView → 重建悬浮按钮
```

#### 扫码功能
- **入口**：错误覆盖层 + 配置浮层，双入口
- **API**：`uni.scanCode({ scanType: ['qrCode'] })`
- **安全防护**：10 秒超时兜底防止按钮卡死，重复点击保护
- **流程**：扫码 → URL 校验 → 去末尾斜杠 → `/health` 验证 → 直接加载（不持久化）

#### 调试日志
扫码和 /health 请求的详细信息写入 `PRIVATE_DOC/df_scan_log.txt`，可通过配置面板的「📋 查看日志」和「🗑 清空日志」按钮管理。

#### 错误提示覆盖层
WebView 加载失败时自动显示错误覆盖层，含红色错误详情文字：
- ⚠️ 红色图标 + 「无法连接服务器」标题
- 红色错误详情（来自 WebView 的 `@error` 事件）
- 当前 URL 卡片 + 操作按钮
  - 「📷 扫码自动绑定」（排首位）
  - 「✏️ 修改服务器地址」→ 打开配置浮层
  - 「🔄 检查网络并重试」→ 重新加载 + 指数退避（2s → 4s → 8s → 15s）
- 网络恢复时自动提示「网络已恢复，点击重试加载」

### 悬浮按钮（⚙）
弃用 Vue DOM 渲染（被 WebView 原生层覆盖），改用 `plus.nativeObj.View` 原生绘制层：
- **创建**：`onReady()` / `onShow()` 时调用 `createFloatBtn()`
- **销毁**：点击后 `destroyFloatBtn()` + `showWebView = false`
- **重建**：关闭面板 / 扫码成功 / 保存成功 / 后台切回时调用 `createFloatBtn()`

### HTTP Cleartext (manifest.json)
```json
"app-plus": {
  "modules": { "Barcode": {} },
  "distribute": {
    "android": {
      "manifestPlugins": { "usesCleartextTraffic": true },
      "permissions": [ "...CAMERA...", "...FLASHLIGHT..." ]
    },
    "ios": {
      "plistcmds": [
        "Set :NSAppTransportSecurity:NSAllowsArbitraryLoads bool true",
        "Set :NSCameraUsageDescription string 需要相机权限以扫描二维码绑定服务器"
      ]
    }
  }
}
```

### App ID
The `appid` in `manifest.json` is auto-generated by HBuilderX on project creation. **Never change it** — it's tied to cloud build certificates.

### 原生导航栏
原生导航栏已**完全隐藏**（`pages.json` 中 `navigationStyle: "custom"`），WebView 全屏沉浸显示，从状态栏下方开始渲染。
- 容器自动添加 `padding-top: env(safe-area-inset-top)` 适配刘海屏
- `@title` 事件已移除，不再同步网页标题

### 启动加载画面
App 冷启动后不立即显示 WebView，而是先显示加载画面：
- 品牌图片（`static/brand.png`）+ CSS 旋转指示器 +「正在连接 DeerFlow 服务器...」文字
- 并行执行 `uni.getNetworkType()` 和 `GET /health` 检查
  - **网络不通** → 显示「📶 设备未连接网络」
  - **连接失败** → 显示「🔌 无法连接服务器」
  - **验证通过** → 淡入 WebView

### WebView 缩放限制
通过 `injectFix()` 注入 viewport meta `user-scalable=no` 和 CSS `touch-action: pan-y`，禁止双指缩放导致页面布局错乱。

### Health API (/health)
DeerFlow 后端提供 `GET /health` 端点，用于 App 内的服务器身份验证（硬限制）。可接受的返回格式：
- `{"status":"healthy","service":"deer-flow-gateway"}` — 标准健康检查
- `{"detail":{"code":"not_authenticated","message":"..."}}` — 需要登录（也算 DeerFlow）
- HTTP 401/403 — 未认证/被拒绝（有端点就算）

URL 拼接时需注意**去末尾斜杠**防双斜杠：`url.replace(/\/+$/, '') + '/health'`

## Important Notes
- The actual app source lives under `DeerFlowApp/DeerFlowApp/` (HBuilderX creates a project subdirectory inside the parent folder)
- The server URL is configured via **`config.js` only** — no persistent storage, edit before cloud build
- DeerFlow frontend updates are reflected automatically in the app (no rebuild needed)
- **App Icon**：唯一源文件为 `static/app-icon.png`（1024×1024 PNG），通过 `generate-icons.ps1` 自动生成各尺寸到 `unpackage/res/icons/`。更换流程：替换 `app-icon.png` → 运行脚本 → 云打包
- No unit tests, no CI/CD — validation is done via device testing
- **零存储策略**：不使用 `getStorageSync` / `setStorageSync` / `removeStorageSync`，config.js 为唯一来源
- **原生按钮**：悬浮按钮使用 `plus.nativeObj.View`，不是 Vue DOM `<view>`
- **URL 硬限制**：通过 `/health` 身份验证（含 not_authenticated 兼容）是加载 URL 的必需条件
- **双斜杠风险**：拼 `/health` 前必须去末尾斜杠，手机上外网双斜杠会 404
- **隐藏导航栏**：`pages.json` 配置 `navigationStyle: "custom"`，WebView 全屏沉浸
- **禁止缩放**：`injectFix()` 注入 `user-scalable=no` + `touch-action: pan-y`
- **启动加载**：冷启动先显示品牌画面 + 旋转指示器，`/health` 验证通过后才显示 WebView

## Bug History
Bug fixes and design decisions are documented in [docs/superpowers/bug-fix-log.md](file:///d:/Wing_D/emto/2026/2026.5/uni-app/docs/superpowers/bug-fix-log.md). Key entries:
- **Bug 1 — 定时器误触发错误覆盖层**：`onShow()` 中多余的 10 秒定时器。已修复，纯事件驱动。
- **Bug 2 — config.js 修改后不生效**：本地存储永远覆盖 config.js。已修复，最终方案：彻底删除所有存储逻辑。
- **Bug 3 — 验证失败时错误信息被覆盖**：testResult 被 openConfigPanel 清空。已修复，交换顺序。
- **Bug 4 — 扫码相机无法自动识别**：`plus.barcode.scan` 在模拟器崩溃。已修复，回退到 `uni.scanCode`。
- **Bug 5 — 悬浮按钮不可见**：Vue DOM 按钮被 WebView 原生层覆盖。已修复，改用 `plus.nativeObj.View` 原生绘制层。
- **Bug 6 — /health 需登录导致验证失败**：not_authenticated 被当成无效。已修复，放宽验证条件。
- **Bug 7 — URL 末尾斜杠导致 /health 404**：双斜杠被路由截断。已修复，`url.replace(/\/+$/, '')`。
